import React from 'react';
import {StyleSheet, Text, View} from 'react-native';
import {RLoader} from '@reusable';

const Logout = () => {
  return (
    <View>
      <RLoader title="Log Out"/>
    </View>
  );
};

export default Logout;

const styles = StyleSheet.create({});
